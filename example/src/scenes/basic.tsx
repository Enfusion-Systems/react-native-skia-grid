// Migrated: the basic scene now lives in ../stories/basic as `BasicStory` and is
// rendered through the story navigator (see example/App.tsx). This thin
// re-export keeps any older import path working.
export { BasicStory as BasicScene } from "../stories/basic";
